
#include "pch.h"
#include "Administrator.h"

/*bool Barry::Administrator::AddPet(Pet^ mascota)
{
	for each (Pet ^ p in lista_mascotas) {
		if (p->Id == mascota->Id) {
			return;
		}
	}
	lista_mascotas->Add(mascota);
}*/